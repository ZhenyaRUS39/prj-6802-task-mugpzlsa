# Plan: prj-6802-task-mugpzlsa

**Project:** `PRJ-4934`  
**Task ID:** `task-mugpzlsa`  
**Repo:** `prj-6802-task-mugpzlsa`  

## Summary

Декомпозирую задачу «Сделай простой блог на Flask» на 4 последовательных subtask: структура проекта и зависимости → модели и БД → маршруты и шаблоны → тесты и README.

## Subtasks

### 1. Структура проекта и зависимости

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать репозиторий prj-6802-task-mugpzlsa, базовую структуру (app.py, templates/, static/, tests/, requirements.txt, .gitignore, README.md). Прописать зависимости: Flask, Flask-SQLAlchemy, Flask-WTF, Flask-Login, pytest. Инициализировать git, сделать первый коммит на ветке main.
- **Test plan:** Клонировать репо, выполнить pip install -r requirements.txt — установка проходит без ошибок; python -c "import flask" возвращает 0; структура каталогов соответствует описанной.

### 2. Модели данных и инициализация БД

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Реализовать модель Post (id, title, slug, body, created_at, updated_at) через SQLAlchemy. Настроить config.py с SECRET_KEY и DATABASE_URI (SQLite по умолчанию). Реализовать фабрику приложения create_app() и инициализацию БД (db.create_all или flask db init/migrate). Добавить seed-скрипт с 2-3 тестовыми постами.
- **Test plan:** Запустить flask --app app run или python -c "from app import create_app; app=create_app(); app.app_context().push(); from models import Post; assert Post.query.count() >= 2" — проверка проходит без исключений.
- **Dependencies:** `sub-1`

### 3. Маршруты, формы и шаблоны

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Реализовать маршруты: GET / (лента постов, пагинация), GET/POST /posts/<slug> (просмотр поста), GET/POST /new (создание поста через WTForms), GET/POST /posts/<slug>/edit (редактирование), POST /posts/<slug>/delete (удаление). Создать базовый шаблон base.html с навигацией и дочерние index.html, post.html, new.html, edit.html. Подключить статику (styles.css).
- **Test plan:** flask run, curl http://127.0.0.1:5000/ —200 OK, HTML содержит заголовки постов; GET /posts/<slug> существующего поста — 200; GET /new — 200, форма рендерится; POST /new с валидными данными редиректит 302 и создаёт пост.
- **Dependencies:** `sub-2`

### 4. Тесты pytest и финальная документация

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Написать pytest-тесты: test_models.py (CRUD над Post), test_routes.py (GET /, GET /posts/<slug>, POST /new, POST /delete). Заполнить README.md: описание, установка (venv + pip install -r requirements.txt), запуск (flask --app app run --debug), переменные окружения, скриншоты/curl-примеры. Обновить requirements.txt с зафиксированными версиями.
- **Test plan:** pytest -v — все тесты зелёные (>= 6 passed); README.md содержит секции Install / Run / Test; запустить команды из README на чистом venv — блог стартует и доступен на http://127.0.0.1:5000/.
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
